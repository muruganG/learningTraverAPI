import { HeighLightPipe } from './heigh-light.pipe';

describe('HeighLightPipe', () => {
  it('create an instance', () => {
    const pipe = new HeighLightPipe();
    expect(pipe).toBeTruthy();
  });
});
